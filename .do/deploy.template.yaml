spec:
  name: amplitude-api-proxy
  services:
    - name: amplitude-api-proxy
      git:
        branch: master
        repo_clone_url: https://github.com/marques-kevin/proxy.amplitude.sudoku.academy
      dockerfile_path: ./Dockerfile
      http_port: 80
